Program(
    FunDclr(
        main,
        storage=none
        params=[]
        type=func(ret_type=int, param_types=[]),
        body=
            WhileStmt(
                main.while.0,
                LitExpr(int, 1):untyped,
                CompoundStmt(
                    SwitchStmt(
                        label=main.switch.1
                        cases=[DefaultCase, IntCase 0, ]
                        LitExpr(int, 1):untyped,
                        CompoundStmt(
                            CaseStmt(
                                LitExpr(int, 0):untyped,
                                BreakStmt(label=main.switch.1);
                            );
                            DefaultStmt(
                                ContinueStmt(label=main.while.0);
                            );
                        );
                    );
                    BreakStmt(label=main.while.0);
                );
            );
            Return(LitExpr(int, 0):untyped, main);
    );
)
